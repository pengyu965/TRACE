# Heavy dependencies for Part 3 (cross-video claim aggregation) only.
# Install on top of requirements.txt when you want to run the aggregator stage:
#
#     pip install -r requirements.txt
#     pip install -r requirements-aggregator.txt
#
# The shared heavy deps (vllm, torch, transformers) are usually already present
# if you also installed requirements-claim-gen.txt. xgrammar is the one extra
# the aggregator needs for constrained JSON generation.

vllm==0.19.0             # pinned for byte-stable re-runs (paper config)
torch>=2.5
transformers>=4.55
xgrammar>=0.1.18         # constrained JSON output for the aggregator LLM
numpy>=1.26
